{% snapshot representante_ventas_snapshot %}

{{
    config(
        target_schema='snapshots',
        unique_key='sales_rep_id',
        strategy='check',
        check_cols=['sales_team_id'],
    )
}}

SELECT
    name_sales_rep,
    sales_rep_id,
    sales_team_id
FROM {{ ref('stg_representante_ventas') }}

{% endsnapshot %}